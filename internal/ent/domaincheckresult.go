// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/kanakmegha/WebsitePingerV2/internal/ent/domaincheckresult"
	"github.com/kanakmegha/WebsitePingerV2/internal/ent/monitorcheck"
)

// DomainCheckResult is the model entity for the DomainCheckResult schema.
type DomainCheckResult struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// CheckID holds the value of the "check_id" field.
	CheckID uuid.UUID `json:"check_id,omitempty"`
	// ExpiryDate holds the value of the "expiry_date" field.
	ExpiryDate time.Time `json:"expiry_date,omitempty"`
	// Registrar holds the value of the "registrar" field.
	Registrar string `json:"registrar,omitempty"`
	// DaysRemaining holds the value of the "days_remaining" field.
	DaysRemaining int `json:"days_remaining,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DomainCheckResultQuery when eager-loading is set.
	Edges        DomainCheckResultEdges `json:"edges"`
	selectValues sql.SelectValues
}

// DomainCheckResultEdges holds the relations/edges for other nodes in the graph.
type DomainCheckResultEdges struct {
	// Check holds the value of the check edge.
	Check *MonitorCheck `json:"check,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// CheckOrErr returns the Check value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e DomainCheckResultEdges) CheckOrErr() (*MonitorCheck, error) {
	if e.Check != nil {
		return e.Check, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: monitorcheck.Label}
	}
	return nil, &NotLoadedError{edge: "check"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DomainCheckResult) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case domaincheckresult.FieldDaysRemaining:
			values[i] = new(sql.NullInt64)
		case domaincheckresult.FieldRegistrar:
			values[i] = new(sql.NullString)
		case domaincheckresult.FieldExpiryDate:
			values[i] = new(sql.NullTime)
		case domaincheckresult.FieldID, domaincheckresult.FieldCheckID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DomainCheckResult fields.
func (dcr *DomainCheckResult) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case domaincheckresult.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				dcr.ID = *value
			}
		case domaincheckresult.FieldCheckID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field check_id", values[i])
			} else if value != nil {
				dcr.CheckID = *value
			}
		case domaincheckresult.FieldExpiryDate:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field expiry_date", values[i])
			} else if value.Valid {
				dcr.ExpiryDate = value.Time
			}
		case domaincheckresult.FieldRegistrar:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field registrar", values[i])
			} else if value.Valid {
				dcr.Registrar = value.String
			}
		case domaincheckresult.FieldDaysRemaining:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field days_remaining", values[i])
			} else if value.Valid {
				dcr.DaysRemaining = int(value.Int64)
			}
		default:
			dcr.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DomainCheckResult.
// This includes values selected through modifiers, order, etc.
func (dcr *DomainCheckResult) Value(name string) (ent.Value, error) {
	return dcr.selectValues.Get(name)
}

// QueryCheck queries the "check" edge of the DomainCheckResult entity.
func (dcr *DomainCheckResult) QueryCheck() *MonitorCheckQuery {
	return NewDomainCheckResultClient(dcr.config).QueryCheck(dcr)
}

// Update returns a builder for updating this DomainCheckResult.
// Note that you need to call DomainCheckResult.Unwrap() before calling this method if this DomainCheckResult
// was returned from a transaction, and the transaction was committed or rolled back.
func (dcr *DomainCheckResult) Update() *DomainCheckResultUpdateOne {
	return NewDomainCheckResultClient(dcr.config).UpdateOne(dcr)
}

// Unwrap unwraps the DomainCheckResult entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (dcr *DomainCheckResult) Unwrap() *DomainCheckResult {
	_tx, ok := dcr.config.driver.(*txDriver)
	if !ok {
		panic("ent: DomainCheckResult is not a transactional entity")
	}
	dcr.config.driver = _tx.drv
	return dcr
}

// String implements the fmt.Stringer.
func (dcr *DomainCheckResult) String() string {
	var builder strings.Builder
	builder.WriteString("DomainCheckResult(")
	builder.WriteString(fmt.Sprintf("id=%v, ", dcr.ID))
	builder.WriteString("check_id=")
	builder.WriteString(fmt.Sprintf("%v", dcr.CheckID))
	builder.WriteString(", ")
	builder.WriteString("expiry_date=")
	builder.WriteString(dcr.ExpiryDate.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("registrar=")
	builder.WriteString(dcr.Registrar)
	builder.WriteString(", ")
	builder.WriteString("days_remaining=")
	builder.WriteString(fmt.Sprintf("%v", dcr.DaysRemaining))
	builder.WriteByte(')')
	return builder.String()
}

// DomainCheckResults is a parsable slice of DomainCheckResult.
type DomainCheckResults []*DomainCheckResult
