// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/kanakmegha/WebsitePingerV2/internal/ent/monitor"
	"github.com/kanakmegha/WebsitePingerV2/internal/ent/tenant"
)

// Monitor is the model entity for the Monitor schema.
type Monitor struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// TenantID holds the value of the "tenant_id" field.
	TenantID uuid.UUID `json:"tenant_id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// URL holds the value of the "url" field.
	URL string `json:"url,omitempty"`
	// Domain holds the value of the "domain" field.
	Domain string `json:"domain,omitempty"`
	// Type holds the value of the "type" field.
	Type monitor.Type `json:"type,omitempty"`
	// EnabledChecks holds the value of the "enabled_checks" field.
	EnabledChecks map[string]bool `json:"enabled_checks,omitempty"`
	// IntervalSeconds holds the value of the "interval_seconds" field.
	IntervalSeconds int `json:"interval_seconds,omitempty"`
	// TimeoutSeconds holds the value of the "timeout_seconds" field.
	TimeoutSeconds int `json:"timeout_seconds,omitempty"`
	// IsActive holds the value of the "is_active" field.
	IsActive bool `json:"is_active,omitempty"`
	// LastStatus holds the value of the "last_status" field.
	LastStatus *monitor.LastStatus `json:"last_status,omitempty"`
	// LastCheckedAt holds the value of the "last_checked_at" field.
	LastCheckedAt *time.Time `json:"last_checked_at,omitempty"`
	// LastAlertSentAt holds the value of the "last_alert_sent_at" field.
	LastAlertSentAt *time.Time `json:"last_alert_sent_at,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the MonitorQuery when eager-loading is set.
	Edges        MonitorEdges `json:"edges"`
	selectValues sql.SelectValues
}

// MonitorEdges holds the relations/edges for other nodes in the graph.
type MonitorEdges struct {
	// Tenant holds the value of the tenant edge.
	Tenant *Tenant `json:"tenant,omitempty"`
	// Checks holds the value of the checks edge.
	Checks []*MonitorCheck `json:"checks,omitempty"`
	// CheckConfigs holds the value of the check_configs edge.
	CheckConfigs []*MonitorCheckConfig `json:"check_configs,omitempty"`
	// Alerts holds the value of the alerts edge.
	Alerts []*Alert `json:"alerts,omitempty"`
	// AlertEvents holds the value of the alert_events edge.
	AlertEvents []*AlertEvent `json:"alert_events,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [5]bool
}

// TenantOrErr returns the Tenant value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e MonitorEdges) TenantOrErr() (*Tenant, error) {
	if e.Tenant != nil {
		return e.Tenant, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: tenant.Label}
	}
	return nil, &NotLoadedError{edge: "tenant"}
}

// ChecksOrErr returns the Checks value or an error if the edge
// was not loaded in eager-loading.
func (e MonitorEdges) ChecksOrErr() ([]*MonitorCheck, error) {
	if e.loadedTypes[1] {
		return e.Checks, nil
	}
	return nil, &NotLoadedError{edge: "checks"}
}

// CheckConfigsOrErr returns the CheckConfigs value or an error if the edge
// was not loaded in eager-loading.
func (e MonitorEdges) CheckConfigsOrErr() ([]*MonitorCheckConfig, error) {
	if e.loadedTypes[2] {
		return e.CheckConfigs, nil
	}
	return nil, &NotLoadedError{edge: "check_configs"}
}

// AlertsOrErr returns the Alerts value or an error if the edge
// was not loaded in eager-loading.
func (e MonitorEdges) AlertsOrErr() ([]*Alert, error) {
	if e.loadedTypes[3] {
		return e.Alerts, nil
	}
	return nil, &NotLoadedError{edge: "alerts"}
}

// AlertEventsOrErr returns the AlertEvents value or an error if the edge
// was not loaded in eager-loading.
func (e MonitorEdges) AlertEventsOrErr() ([]*AlertEvent, error) {
	if e.loadedTypes[4] {
		return e.AlertEvents, nil
	}
	return nil, &NotLoadedError{edge: "alert_events"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Monitor) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case monitor.FieldEnabledChecks:
			values[i] = new([]byte)
		case monitor.FieldIsActive:
			values[i] = new(sql.NullBool)
		case monitor.FieldIntervalSeconds, monitor.FieldTimeoutSeconds:
			values[i] = new(sql.NullInt64)
		case monitor.FieldName, monitor.FieldURL, monitor.FieldDomain, monitor.FieldType, monitor.FieldLastStatus:
			values[i] = new(sql.NullString)
		case monitor.FieldLastCheckedAt, monitor.FieldLastAlertSentAt, monitor.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case monitor.FieldID, monitor.FieldTenantID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Monitor fields.
func (m *Monitor) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case monitor.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				m.ID = *value
			}
		case monitor.FieldTenantID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value != nil {
				m.TenantID = *value
			}
		case monitor.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				m.Name = value.String
			}
		case monitor.FieldURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field url", values[i])
			} else if value.Valid {
				m.URL = value.String
			}
		case monitor.FieldDomain:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field domain", values[i])
			} else if value.Valid {
				m.Domain = value.String
			}
		case monitor.FieldType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field type", values[i])
			} else if value.Valid {
				m.Type = monitor.Type(value.String)
			}
		case monitor.FieldEnabledChecks:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field enabled_checks", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &m.EnabledChecks); err != nil {
					return fmt.Errorf("unmarshal field enabled_checks: %w", err)
				}
			}
		case monitor.FieldIntervalSeconds:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field interval_seconds", values[i])
			} else if value.Valid {
				m.IntervalSeconds = int(value.Int64)
			}
		case monitor.FieldTimeoutSeconds:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field timeout_seconds", values[i])
			} else if value.Valid {
				m.TimeoutSeconds = int(value.Int64)
			}
		case monitor.FieldIsActive:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_active", values[i])
			} else if value.Valid {
				m.IsActive = value.Bool
			}
		case monitor.FieldLastStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field last_status", values[i])
			} else if value.Valid {
				m.LastStatus = new(monitor.LastStatus)
				*m.LastStatus = monitor.LastStatus(value.String)
			}
		case monitor.FieldLastCheckedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_checked_at", values[i])
			} else if value.Valid {
				m.LastCheckedAt = new(time.Time)
				*m.LastCheckedAt = value.Time
			}
		case monitor.FieldLastAlertSentAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_alert_sent_at", values[i])
			} else if value.Valid {
				m.LastAlertSentAt = new(time.Time)
				*m.LastAlertSentAt = value.Time
			}
		case monitor.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				m.CreatedAt = value.Time
			}
		default:
			m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Monitor.
// This includes values selected through modifiers, order, etc.
func (m *Monitor) Value(name string) (ent.Value, error) {
	return m.selectValues.Get(name)
}

// QueryTenant queries the "tenant" edge of the Monitor entity.
func (m *Monitor) QueryTenant() *TenantQuery {
	return NewMonitorClient(m.config).QueryTenant(m)
}

// QueryChecks queries the "checks" edge of the Monitor entity.
func (m *Monitor) QueryChecks() *MonitorCheckQuery {
	return NewMonitorClient(m.config).QueryChecks(m)
}

// QueryCheckConfigs queries the "check_configs" edge of the Monitor entity.
func (m *Monitor) QueryCheckConfigs() *MonitorCheckConfigQuery {
	return NewMonitorClient(m.config).QueryCheckConfigs(m)
}

// QueryAlerts queries the "alerts" edge of the Monitor entity.
func (m *Monitor) QueryAlerts() *AlertQuery {
	return NewMonitorClient(m.config).QueryAlerts(m)
}

// QueryAlertEvents queries the "alert_events" edge of the Monitor entity.
func (m *Monitor) QueryAlertEvents() *AlertEventQuery {
	return NewMonitorClient(m.config).QueryAlertEvents(m)
}

// Update returns a builder for updating this Monitor.
// Note that you need to call Monitor.Unwrap() before calling this method if this Monitor
// was returned from a transaction, and the transaction was committed or rolled back.
func (m *Monitor) Update() *MonitorUpdateOne {
	return NewMonitorClient(m.config).UpdateOne(m)
}

// Unwrap unwraps the Monitor entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (m *Monitor) Unwrap() *Monitor {
	_tx, ok := m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Monitor is not a transactional entity")
	}
	m.config.driver = _tx.drv
	return m
}

// String implements the fmt.Stringer.
func (m *Monitor) String() string {
	var builder strings.Builder
	builder.WriteString("Monitor(")
	builder.WriteString(fmt.Sprintf("id=%v, ", m.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", m.TenantID))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(m.Name)
	builder.WriteString(", ")
	builder.WriteString("url=")
	builder.WriteString(m.URL)
	builder.WriteString(", ")
	builder.WriteString("domain=")
	builder.WriteString(m.Domain)
	builder.WriteString(", ")
	builder.WriteString("type=")
	builder.WriteString(fmt.Sprintf("%v", m.Type))
	builder.WriteString(", ")
	builder.WriteString("enabled_checks=")
	builder.WriteString(fmt.Sprintf("%v", m.EnabledChecks))
	builder.WriteString(", ")
	builder.WriteString("interval_seconds=")
	builder.WriteString(fmt.Sprintf("%v", m.IntervalSeconds))
	builder.WriteString(", ")
	builder.WriteString("timeout_seconds=")
	builder.WriteString(fmt.Sprintf("%v", m.TimeoutSeconds))
	builder.WriteString(", ")
	builder.WriteString("is_active=")
	builder.WriteString(fmt.Sprintf("%v", m.IsActive))
	builder.WriteString(", ")
	if v := m.LastStatus; v != nil {
		builder.WriteString("last_status=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	if v := m.LastCheckedAt; v != nil {
		builder.WriteString("last_checked_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	if v := m.LastAlertSentAt; v != nil {
		builder.WriteString("last_alert_sent_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Monitors is a parsable slice of Monitor.
type Monitors []*Monitor
