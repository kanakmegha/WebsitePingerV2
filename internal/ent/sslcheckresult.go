// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/kanakmegha/WebsitePingerV2/internal/ent/monitorcheck"
	"github.com/kanakmegha/WebsitePingerV2/internal/ent/sslcheckresult"
)

// SSLCheckResult is the model entity for the SSLCheckResult schema.
type SSLCheckResult struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// CheckID holds the value of the "check_id" field.
	CheckID uuid.UUID `json:"check_id,omitempty"`
	// ExpiryDate holds the value of the "expiry_date" field.
	ExpiryDate time.Time `json:"expiry_date,omitempty"`
	// Issuer holds the value of the "issuer" field.
	Issuer string `json:"issuer,omitempty"`
	// Valid holds the value of the "valid" field.
	Valid bool `json:"valid,omitempty"`
	// DaysRemaining holds the value of the "days_remaining" field.
	DaysRemaining int `json:"days_remaining,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SSLCheckResultQuery when eager-loading is set.
	Edges        SSLCheckResultEdges `json:"edges"`
	selectValues sql.SelectValues
}

// SSLCheckResultEdges holds the relations/edges for other nodes in the graph.
type SSLCheckResultEdges struct {
	// Check holds the value of the check edge.
	Check *MonitorCheck `json:"check,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// CheckOrErr returns the Check value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SSLCheckResultEdges) CheckOrErr() (*MonitorCheck, error) {
	if e.Check != nil {
		return e.Check, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: monitorcheck.Label}
	}
	return nil, &NotLoadedError{edge: "check"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SSLCheckResult) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case sslcheckresult.FieldValid:
			values[i] = new(sql.NullBool)
		case sslcheckresult.FieldDaysRemaining:
			values[i] = new(sql.NullInt64)
		case sslcheckresult.FieldIssuer:
			values[i] = new(sql.NullString)
		case sslcheckresult.FieldExpiryDate:
			values[i] = new(sql.NullTime)
		case sslcheckresult.FieldID, sslcheckresult.FieldCheckID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SSLCheckResult fields.
func (scr *SSLCheckResult) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case sslcheckresult.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				scr.ID = *value
			}
		case sslcheckresult.FieldCheckID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field check_id", values[i])
			} else if value != nil {
				scr.CheckID = *value
			}
		case sslcheckresult.FieldExpiryDate:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field expiry_date", values[i])
			} else if value.Valid {
				scr.ExpiryDate = value.Time
			}
		case sslcheckresult.FieldIssuer:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field issuer", values[i])
			} else if value.Valid {
				scr.Issuer = value.String
			}
		case sslcheckresult.FieldValid:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field valid", values[i])
			} else if value.Valid {
				scr.Valid = value.Bool
			}
		case sslcheckresult.FieldDaysRemaining:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field days_remaining", values[i])
			} else if value.Valid {
				scr.DaysRemaining = int(value.Int64)
			}
		default:
			scr.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SSLCheckResult.
// This includes values selected through modifiers, order, etc.
func (scr *SSLCheckResult) Value(name string) (ent.Value, error) {
	return scr.selectValues.Get(name)
}

// QueryCheck queries the "check" edge of the SSLCheckResult entity.
func (scr *SSLCheckResult) QueryCheck() *MonitorCheckQuery {
	return NewSSLCheckResultClient(scr.config).QueryCheck(scr)
}

// Update returns a builder for updating this SSLCheckResult.
// Note that you need to call SSLCheckResult.Unwrap() before calling this method if this SSLCheckResult
// was returned from a transaction, and the transaction was committed or rolled back.
func (scr *SSLCheckResult) Update() *SSLCheckResultUpdateOne {
	return NewSSLCheckResultClient(scr.config).UpdateOne(scr)
}

// Unwrap unwraps the SSLCheckResult entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (scr *SSLCheckResult) Unwrap() *SSLCheckResult {
	_tx, ok := scr.config.driver.(*txDriver)
	if !ok {
		panic("ent: SSLCheckResult is not a transactional entity")
	}
	scr.config.driver = _tx.drv
	return scr
}

// String implements the fmt.Stringer.
func (scr *SSLCheckResult) String() string {
	var builder strings.Builder
	builder.WriteString("SSLCheckResult(")
	builder.WriteString(fmt.Sprintf("id=%v, ", scr.ID))
	builder.WriteString("check_id=")
	builder.WriteString(fmt.Sprintf("%v", scr.CheckID))
	builder.WriteString(", ")
	builder.WriteString("expiry_date=")
	builder.WriteString(scr.ExpiryDate.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("issuer=")
	builder.WriteString(scr.Issuer)
	builder.WriteString(", ")
	builder.WriteString("valid=")
	builder.WriteString(fmt.Sprintf("%v", scr.Valid))
	builder.WriteString(", ")
	builder.WriteString("days_remaining=")
	builder.WriteString(fmt.Sprintf("%v", scr.DaysRemaining))
	builder.WriteByte(')')
	return builder.String()
}

// SSLCheckResults is a parsable slice of SSLCheckResult.
type SSLCheckResults []*SSLCheckResult
